# Entryway Bot

This is the Python bot for the Entryway application. It relies on the web app (`apps/web`) for core functionality.

## Setup

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Running

```bash
python main.py
```
